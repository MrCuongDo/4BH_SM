extends layout

block content
    h1= title
    p Welcome to #{title}

    form(action="") 
        table 
            thead
            tbody
                tr 
                    td
                        input#inputNewItem(type="text")
                        input#themBtn(type="button", value="Them")
    
    script. 
        $('#themBtn').on('click', function(e) {
            let test = $('#inputNewItem').val()
            console.log(test)
        })

